[package]
name = "gen-datasets"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
buffa = { path = "../../buffa" }
rand = "0.9"

[build-dependencies]
buffa-build = { path = "../../buffa-build" }
