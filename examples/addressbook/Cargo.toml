[package]
name = "example-addressbook"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
buffa = { path = "../../buffa" }
buffa-types = { path = "../../buffa-types" }

[build-dependencies]
buffa-build = { path = "../../buffa-build" }
